import { describe,expect,it } from 'vitest';
import { readFileSync } from 'node:fs'; import { resolve } from 'node:path';
const migration=readFileSync(resolve(process.cwd(),'supabase/migrations/0004_cdr_read_only.sql'),'utf8');
const cdrView=readFileSync(resolve(process.cwd(),'components/cdr-console.tsx'),'utf8');
describe('rôles opérationnels',()=>{it('retire toute politique d’écriture CDR sur les occupations',()=>{expect(migration).toContain('drop policy if exists "table editors manage occupancy"');expect(migration).toContain("create or replace function public.can_edit_table");expect(migration).toContain("select public.current_role() in ('admin','hostess')")});it('restreint la lecture CDR à ses propres tables',()=>{expect(migration).toContain("public.current_role()='head_waiter'");expect(migration).toContain('p.head_waiter_id=t.head_waiter_id')});it('rend la vue CDR sans formulaire ni éditeur',()=>{expect(cdrView).not.toContain('TableEditor');expect(cdrView).not.toContain('<input');expect(cdrView).not.toContain('upsert(')})});

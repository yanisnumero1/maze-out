import { createClient } from '@supabase/supabase-js'; import type { LiveTable } from './types';
export async function getLiveTables():Promise<LiveTable[]> { const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)return []; const db=createClient(url,key);const {data}=await db.from('tables').select('*, zone:zones(*), head_waiter:head_waiters(*), reservation:reservations(*), occupancy:occupancies(*)').eq('active',true).order('number');return (data??[]).map((t:any)=>({...t,reservation:Array.isArray(t.reservation)?t.reservation[0]??null:t.reservation,occupancy:Array.isArray(t.occupancy)?t.occupancy[0]??null:t.occupancy})); }
